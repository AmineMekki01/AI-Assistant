"""Side-effecting workflows composed from atomic tools.

Register multi-step, user-visible workflows here with ``@action``. The central
capability catalogue decides which action modules are loaded at runtime.
"""
